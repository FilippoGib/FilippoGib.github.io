// AI Services removed.
// This file is kept empty to prevent import errors if referenced elsewhere.

export const streamChatResponse = async () => {
  throw new Error("AI features are disabled.");
};