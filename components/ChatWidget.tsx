import React from 'react';

// AI Assistant removed as per request.
// This component is kept empty to prevent import errors if referenced elsewhere,
// but it renders nothing.

const ChatWidget: React.FC = () => {
  return null;
};

export default ChatWidget;